# storage:
#   files:
#     - path: /etc/ssh/sshd_config.d/60-enable-passwords.conf
#       mode: 0644
#       contents:
#         inline: |
#           # Fedora CoreOS disables SSH password login by default.
#           # Re-enable it.
#           # This file must sort before `40-disable-passwords.conf`.
#           PasswordAuthentication yes
